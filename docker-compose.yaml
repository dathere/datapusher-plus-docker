version: "3"

volumes:
  pg_data:

services:
  datapusher:
    container_name: datapusher-plus
    build:
      context: ./datapusher-plus/0.15.0/
      dockerfile: Dockerfile
    restart: always
    ports:
      - "8800:8800"
    network_mode: "host"
    environment:
      - WRITE_ENGINE_URL=postgresql://dathere_datapusher:-Vcvn3Kbq4wiAEkA@localhost/data_dathere_datastore_default
      - SQLALCHEMY_DATABASE_URI=postgresql://dathere_datapusher_jobs:Uu6c-YFr9A_kFE4Z@$PG_HOST:5432/dathere_datapusher_jobs